---
name: hello
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: salahuddin
        run: echo "git actions"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: salahuddin
        run: echo "test"
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: salahuddin
        run: echo "deploy"
